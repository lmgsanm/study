# 常用容器镜像源

## 1. 阿里云

```
registry.aliyuncs.com
```

## 2. 网易云

```
hub-mirror.c.163.com
```

## 3. 百度云

```
mirror.baidubce.com
```

## 4. DaoCloud

```
docker.m.daocloud.io
```

# daemon.json

```
cat <<EOF > /etc/docker/daemon.json
{
  "registry-mirrors": [
    "https://registry.aliyuncs.com",
    "https://hub-mirror.c.163.com",
    "https://mirror.baidubce.com"
  ]
}
EOF

systemctl daemon-reload
systemctl restart docker
```

# 常用 K8s 镜像前缀映射

- `registry.k8s.io` → `registry.aliyuncs.com/google_containers`
- `quay.io` → `registry.aliyuncs.com/quayio`
- `gcr.io` → `registry.aliyuncs.com/gcr`
- `docker.io` → `docker.m.daocloud.io`

# 搜索国内镜像

```bash
yum install -y skopeo
skopeo list-tags docker://registry.aliyuncs.com/google_containers/ingress-nginx-controller

```

