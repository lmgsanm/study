

# K8S集群配置Hosts

```
kubectl edit configmap coredns -n kube-system
```

Sanm@2026

```
apiVersion: v1
data:
  Corefile: |
    .:53 {
        errors
        health {
           lameduck 5s
        }
        ready
        kubernetes cluster.local in-addr.arpa ip6.arpa {
           pods insecure
           fallthrough in-addr.arpa ip6.arpa
           ttl 30
        }
        host {
           47.85.132.146        gitlab.example.com
           47.85.132.146        minio.example.com
           47.85.132.146        registry.example.com
           47.85.132.146        kas.example.com
           47.85.132.146        argocd.example.com
           fallthrough
        }
        prometheus :9153
        forward . /etc/resolv.conf {
           max_concurrent 1000
        }
        cache 30
        loop
        reload
        loadbalance
    }

```



```
helm upgrade gitlab gitlab/gitlab -n gitlab -f values.yaml \
  --set global.hosts.gitlab.host=gitlab.example.com \
  --set global.hosts.domain=example.com \
  --set global.ingress.tls.enabled=false \
  --set certmanager.install=false \
  --set gitlab.webservice.ingress.annotations."nginx.ingress.kubernetes.io/ssl-redirect"=false \
  --set gitlab.webservice.workhorse.enabled=true
  
 kubectl delete pods --all -n gitlab
 
 
```





helm install runner-k8s gitlab/gitlab-runner -f values.yaml -n gitlab-runner --create-namespace

```
[root@kube-master gitlab-runner]# helm upgrade runner-k8s gitlab/gitlab-runner -n gitlab-runner -f values.yaml  --set runners.privileged=true
Release "runner-k8s" has been upgraded. Happy Helming!
NAME: runner-k8s
LAST DEPLOYED: Wed Apr 15 22:43:41 2026
NAMESPACE: gitlab-runner
STATUS: deployed
REVISION: 2
TEST SUITE: None
NOTES:
Your GitLab Runner should now be registered against the GitLab instance reachable at: "http://gitlab.example.com"
[root@kube-master gitlab-runner]# kubectl delete pod --all -n gitlab-runner
[root@kube-master gitlab-runner]# kubectl get configmap  -n  gitlab-runner
NAME                       DATA   AGE
kube-root-ca.crt           1      25h
runner-k8s-gitlab-runner   6      36m
[root@kube-master gitlab-runner]# kubectl edit configmap runner-k8s-gitlab-runner -n  gitlab-runner

```



```
runners:
  config: |
    [[runners]]
      [runners.kubernetes]
        namespace = "{{.Release.Namespace}}"
        image = "ubuntu:18.04"
        privileged = true
        [[runners.kubernetes.volumes.host_path]]
          name = "docker"
          mount_path = "/var/run/docker.sock"
          
```



```
[runners.kubernetes]
  privileged = false # Usually works without privileged if permissions are correct
  [[runners.kubernetes.volumes.host_path]]
    name = "docker-sock"
    mount_path = "/var/run/docker.sock"
    host_path = "/var/run/docker.sock"
```



```
  config: |
    [[runners]]
      [runners.kubernetes]
        namespace = "{{ default .Release.Namespace .Values.runners.jobNamespace }}"
        image = "alpine"
         privileged = true

         [[runners.kubernetes.volumes.host_path]]
            name = "docker-sock"
            mount_path = "/var/run/docker.sock"
            host_path = "/var/run/docker.sock"

```

docker:18.09.7-dind



```
image: docker:latest

variables:
 DOCKER_HOST: tcp://docker:2376
 DOCKER_DRIVER: overlay2
 DOCKER_TLS_CERTDIR: "/certs"
 DOCKER_TLS_VERIFY: 1
 DOCKER_CERT_PATH: "$DOCKER_TLS_CERTDIR/client"


services:
  - name: docker:dind
    entrypoint: ["env", "-u", "DOCKER_HOST"]
    command: ["dockerd-entrypoint.sh"]
    alias: docker


```



```
variables:
  IMAGE_PREFIX: "registry.example.com/your-project"
  GITLAB_URL: "http://gitlab.example.com"
  ARGOCD_SERVER: "argocd.example.com"
  PROJECT_NAME: "demo"


stages:          # List of stages for jobs, and their order of execution
  - build
  # - test
  # - deploy

cache:
  paths:
    - frontend/node_modules
    - backend/go/pkg

scan_code:
  stage: build
  tags: 
    - k8s
  image: golang:1.22-alpine
  script:
    - echo "=== 代码扫描（go vet + 静态检查）==="
    - cd backend
    - go vet ./...
    - echo "代码扫描完成"
build_frontend:
  stage: build
  image: docker:18.09.7-dind
  services:
    - docker:18.09.7-dind
  # variables:
  #   # 核心：强制使用 TCP 连接 DinD，不使用 sock
  #   DOCKER_HOST: tcp://docker:2375
  #   DOCKER_TLS_CERTDIR: ""
  #   DOCKER_DRIVER: overlay2
  script:
    - echo "=== 构建 Frontend 镜像 ==="
    - docker info
    - docker build -t ${IMAGE_PREFIX}/frontend:${CI_COMMIT_SHORT_SHA} -f frontend/Dockerfile frontend/
    - docker tag ${IMAGE_PREFIX}/frontend:${CI_COMMIT_SHORT_SHA} ${IMAGE_PREFIX}/frontend:latest
  tags:
    - k8s


# unit-test-job:   # This job runs in the test stage.
#   stage: test    # It only starts when the job in the build stage completes successfully.
#   tags: 
#     - k8s
#   script:
#     - echo "Running unit tests... This will take about 60 seconds."
#     - sleep 60
#     - echo "Code coverage is 90%"

# lint-test-job:   # This job also runs in the test stage.
#   stage: test    # It can run at the same time as unit-test-job (in parallel).
#   tags: 
#     - k8s
#   script:
#     - echo "Linting code... This will take about 10 seconds."
#     - sleep 10
#     - echo "No lint issues found."

# deploy-job:      # This job runs in the deploy stage.
#   stage: deploy  # It only runs when *both* jobs in the test stage complete successfully.
#   tags: 
#     - k8s
#   environment: production
#   script:
#     - echo "Deploying application..."
#     - echo "Application successfully deployed."

```



## GOLANG项目的单元测试

### 前提：编写 Go 单元测试

1. **测试文件**：与源码同目录，命名为 `*_test.go`

2. 测试函数

   ：命名 

   ```
   TestXxx(t *testing.T)
   ```

   go

   

   运行

   

   

   

   

   ```
   // math.go
   func Add(a, b int) int { return a + b }
   
   // math_test.go
   package main
   
   import "testing"
   
   func TestAdd(t *testing.T) {
       if Add(1, 2) != 3 {
           t.Error("1+2 should be 3")
       }
   }
   ```

   

3. 运行命令

   （本地）：

   bash

   

   运行

   

   

   

   

   ```
   go test -v ./...          # 跑所有测试
   go test -race ./...       # 检测竞态条件
   go test -cover ./...      # 覆盖率
   ```

   

------

### 二、最简 GitLab CI 配置（推荐）

在项目根目录创建 **`.gitlab-ci.yml`**：

yaml











```
# 1. 使用官方 Go 镜像
image: golang:1.22  # 用你项目的 Go 版本

stages:
  - test

# 2. 单元测试任务
unit-test:
  stage: test
  before_script:
    # 可选：加速依赖下载
    - go env -w GOPROXY=https://goproxy.io,direct
    # 下载项目依赖
    - go mod download
  script:
    # 代码格式化检查（可选）
    - go fmt ./...
    # 静态检查（可选）
    - go vet ./...
    # 核心：运行所有测试（带竞态检测、失败即停）
    - go test -v -failfast -race -coverprofile=coverage.out ./...
    # 查看覆盖率摘要（可选）
    - go tool cover -func=coverage.out
  # 3. 缓存依赖，加速下次构建
  cache:
    paths:
      - .go/pkg/mod/
  # 4. 始终上传产物（即使测试失败）
  artifacts:
    when: always
    paths:
      - coverage.out
    reports:
      # 支持 GitLab 内置测试可视化（需 gotestsum）
      junit: report.xml
```

------

### 三、进阶：生成 GitLab 可视化测试报告

原生 `go test` 不支持 JUnit XML，用 **`gotestsum`** 生成：

yaml











```
unit-test:
  stage: test
  image: golang:1.22
  script:
    - go env -w GOPROXY=https://goproxy.io,direct
    - go mod download
    # 安装 gotestsum
    - go install gotest.tools/gotestsum@latest
    # 跑测试并生成 JUnit 报告
    - gotestsum --junitfile report.xml --format testname -- -race -cover ./...
  artifacts:
    when: always
    reports:
      junit: report.xml  # GitLab 自动解析
```

效果：

- GitLab → 你的项目 → **构建 → 测试报告**
- 可看通过率、失败用例、耗时

------

### 四、完整企业级配置（含缓存、覆盖率、多阶段）

yaml











```
image: golang:1.22

# 定义缓存模板（复用）
.go-cache:
  cache:
    paths:
      - .go/pkg/mod/
    key:
      files:
        - go.mod
        - go.sum

stages:
  - lint
  - test
  - build

# 1. 代码质量检查
lint:
  stage: lint
  image: golangci/golangci-lint:latest
  extends: .go-cache
  script:
    - golangci-lint run ./... --timeout 5m

# 2. 单元测试（带报告+覆盖率）
test:
  stage: test
  extends: .go-cache
  script:
    - go env -w GOPROXY=https://goproxy.io,direct
    - go mod download
    - go install gotest.tools/gotestsum@latest
    - gotestsum --junitfile report.xml -- -v -race -coverprofile=coverage.out ./...
    - go tool cover -html=coverage.out -o coverage.html
  artifacts:
    when: always
    paths:
      - coverage.html
    reports:
      junit: report.xml

# 3. 构建（可选）
build:
  stage: build
  extends: .go-cache
  script:
    - go build -o app ./cmd/...
  artifacts:
    paths:
      - app
```

------

### 五、关键参数说明

- **`go test -v`**：详细日志
- **`-failfast`**：第一个失败就停（CI 推荐）
- **`-race`**：开启数据竞态检测（必加）
- **`-coverprofile`**：生成覆盖率文件
- **`gotestsum`**：生成 GitLab 支持的 JUnit 报告
- **`cache`**：缓存 `go mod` 依赖，大幅提速
- **`artifacts.reports.junit`**：开启 GitLab 测试可视化

------

### 六、常见问题

1. **测试不失败（panic 也过）**

   

   - 必须加 **`-race`** 或 **`-failfast`**，强制非零退出码
   - 用 `gotestsum` 更可靠

   

2. **依赖下载慢**

   

   - 加 `GOPROXY`：`go env -w GOPROXY=https://goproxy.io,direct`

   

3. **私有依赖拉不到**

   

   - 在 CI/CD 设置中配置 **Git 凭据 / SSH 密钥**

   

4. **报告不显示**

   

   - 确认 `artifacts.reports.junit` 指向正确 XML
   - 检查 GitLab 版本（需 12.4+）



## sonar扫描

在 GitLab CI 中为 Go 项目集成 SonarQube/SonarCloud 代码扫描，核心流程是：**运行单元测试 → 生成测试报告与覆盖率 → 用 SonarScanner CLI 上传分析 → 结果同步回 GitLab MR**。下面是完整可直接复制的配置方案。

### 一、准备工作（管理员 / 配置一次）

#### 1. 在 SonarQube 创建项目

1. 登录 SonarQube → **Create project → GitLab**（或手动）

2. 获取：

   - `Project key` (如 `my-go-app`)
   - `SONAR_TOKEN` (生成令牌，**保密**)
   - `SONAR_HOST_URL` (如 `https://sonar.example.com`)

   

#### 2. GitLab 项目设置 CI 变量

项目 → **Settings → CI/CD → Variables**，添加：

- `SONAR_TOKEN`：值为上面的令牌（**勾选 Protect & Mask**）
- `SONAR_HOST_URL`：SonarQube 地址

------

### 二、项目根目录添加配置文件

#### 1. sonar-project.properties（必选）

properties











```
# 项目标识
sonar.projectKey=my-go-app          # 替换为你的 Project key
sonar.projectName=my-go-app
sonar.projectVersion=1.0

# 源码路径（Go 项目必须设为 .）
sonar.sources=.
sonar.exclusions=**/vendor/**,**/*_test.go  # 排除测试和 vendor

# 测试文件配置
sonar.tests=.
sonar.test.inclusions=**/*_test.go

# Go 覆盖率报告（由 go test 生成）
sonar.go.coverage.reportPaths=coverage.out

# 测试执行报告（可选，需 gotestsum）
sonar.go.tests.reportPaths=report.xml

# 编码与语言
sonar.sourceEncoding=UTF-8
sonar.language=go
```

#### 2. .gitlab-ci.yml（核心流水线）

yaml











```
stages:
  - test
  - sonar

# 缓存 Go 依赖与 Sonar 缓存（加速）
cache:
  paths:
    - .go/pkg/mod/
    - .sonar/cache/

variables:
  # 禁用浅克隆（Sonar 需要完整 Git 历史）
  GIT_DEPTH: "0"
  # Go 模块代理
  GOPROXY: "https://goproxy.io,direct"

# 1. 测试阶段：生成测试报告 + 覆盖率
go-test:
  stage: test
  image: golang:1.22  # 匹配你的 Go 版本
  script:
    - go mod download
    # 安装 gotestsum（生成 JUnit 报告）
    - go install gotest.tools/gotestsum@latest
    # 运行测试：带竞态检测 + 覆盖率 + JUnit 报告
    - gotestsum --junitfile report.xml --format testname -- -v -race -coverprofile=coverage.out ./...
    # 查看覆盖率摘要
    - go tool cover -func=coverage.out
  artifacts:
    # 测试失败也保留报告给 Sonar
    when: always
    paths:
      - coverage.out
      - report.xml
    reports:
      junit: report.xml  # GitLab 内置测试报告

# 2. Sonar 扫描阶段
sonar-check:
  stage: sonar
  image: sonarsource/sonar-scanner-cli:latest
  needs: [go-test]  # 依赖测试完成
  script:
    - sonar-scanner
        -Dsonar.projectKey=my-go-app  # 与 properties 一致
        -Dsonar.token=$SONAR_TOKEN
        -Dsonar.host.url=$SONAR_HOST_URL
        -Dsonar.qualitygate.wait=true  # 等待质量门禁结果
  allow_failure: false  # 质量门禁失败则 pipeline 失败
  # 仅在主分支和合并请求触发
  rules:
    - if: $CI_COMMIT_BRANCH == "main" || $CI_PIPELINE_SOURCE == "merge_request_event"
```

------

### 三、关键配置说明

#### 1. Go 报告生成

- **测试报告**：`gotestsum` → `report.xml`（JUnit 格式）

- **覆盖率**：`go test -coverprofile=coverage.out` → Sonar 原生支持SonarQube

- 必加参数

  ：

  - `-race`：检测数据竞争（Go 必开）
  - `-coverprofile`：覆盖率文件
  - `GIT_DEPTH:0`：Sonar 需要完整 Git 历史SonarQube

  

#### 2. Sonar 关键参数

- `sonar.sources=.`：Go 项目必须设为根目录（含 `go.mod`）SonarQube
- `sonar.exclusions`：排除 `_test.go` 和 `vendor`SonarQube
- `sonar.qualitygate.wait=true`：**阻塞到质量门禁出结果**SonarQube
- `allow_failure: false`：代码不达标则 CI 失败（推荐）

------

### 四、GitLab MR 集成（最佳实践）

#### 1. 开启 MR 装饰（Sonar 评论）

SonarQube → **Administration → DevOps Platform Integrations → GitLab**

- 填入 GitLab URL、管理员 Token
- 项目 → **Project Settings → DevOps Platform Integration** → 绑定 GitLab 项目SonarQube

效果：

- MR 自动显示：✅ Passed / ❌ Failed
- 直接在代码行标注 Bug、漏洞、坏味道

#### 2. 阻止不合格代码合并

GitLab → **Settings → Merge requests → Merge checks**

- 勾选 **Pipelines must succeed**
- 勾选 **All threads must be resolved**SonarQube

------

### 五、常见问题与排错

1. **Sonar 显示 0 文件 / 0 覆盖率**

   

   - 确认 `sonar.sources=.`、`go.mod` 在根目录
   - 检查 `coverage.out` 存在且非空
   - 路径正确：`sonar.go.coverage.reportPaths=coverage.out`SonarQube

   

2. **权限错误 / 连接失败**

   

   - 确认 `SONAR_TOKEN` 正确、已 Mask
   - 确认 GitLab Runner 能访问 `SONAR_HOST_URL`

   

3. **覆盖率偏低**

   

   - 运行 `go tool cover -html=coverage.out` 本地查看
   - 确认测试覆盖核心业务逻辑

   

4. **Sonar 扫描慢**

   

   - 开启缓存：`.sonar/cache/`
   - 排除 `vendor/`、`dist/`、`testdata/`

   

------

### 六、完整效果

- GitLab CI 流水线：**Test → Sonar**
- 测试报告：GitLab → **Build → Test report**
- 质量报告：Sonar 面板 + **GitLab MR 评论**
- 门禁：质量不达标 → **合并被阻止**



Trivy 是**轻量、无依赖、支持 GitLab 集成**的镜像安全扫描工具，专门扫：

- 镜像漏洞（CVE）
- 敏感信息（密钥、密码）
- 配置错误
- Go 代码依赖漏洞

我给你一套**开箱即用、企业级、带门禁 + 报告**的 GitLab CI 配置，Go 项目直接复制就能用。

------

## 一、整体流程

1. 构建 Go 项目镜像
2. **Trivy 扫描镜像**
3. 漏洞严重则**流水线失败（门禁）**
4. 生成 GitLab 可直接查看的**安全报告**

------

## 二、最终可用 .gitlab-ci.yml

yaml











```
# 定义流水线阶段
stages:
  - build
  - scan

# 全局变量（根据你的项目修改）
variables:
  IMAGE_NAME: "my-go-app"          # 你的镜像名
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA  # 用 commit 哈希做标签
  DOCKER_REGISTRY: $CI_REGISTRY    # GitLab 自带镜像仓库
  FULL_IMAGE: $DOCKER_REGISTRY/$CI_PROJECT_PATH/$IMAGE_NAME:$IMAGE_TAG

# 缓存加速
cache:
  paths:
    - .trivy/

# ======================
# 1. 构建 Go 项目镜像
# ======================
build-go-image:
  stage: build
  image: docker:24-dind
  services:
    - docker:24-dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $FULL_IMAGE .
    - docker push $FULL_IMAGE
  only:
    - main
    - merge_requests

# ======================
# 2. TRIVY 镜像安全扫描（核心）
# ======================
trivy-image-scan:
  stage: scan
  image: aquasec/trivy:latest
  dependencies: []
  script:
    # 登录镜像仓库（必须）
    - trivy login $CI_REGISTRY -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD
    
    # 扫描镜像 → 生成 GitLab 兼容的安全报告
    - trivy image
        --format gitlab
        --output gl-container-scanning-report.json
        --severity HIGH,CRITICAL       # 只拦截高危/严重漏洞
        --exit-code 1                   # 发现漏洞 → 流水线失败（门禁）
        --no-progress
        $FULL_IMAGE

  # 始终上传报告（即使扫描失败）
  artifacts:
    reports:
      container_scanning: gl-container-scanning-report.json
    paths:
      - gl-container-scanning-report.json
    when: always

  only:
    - main
    - merge_requests
```

------

## 三、关键配置解释（你必须懂）

### 1. 门禁规则

yaml











```
--severity HIGH,CRITICAL   # 只拦截高危/严重漏洞
--exit-code 1              # 发现就失败，阻止合并
```

- 想宽松一点：去掉 `--exit-code 1`
- 想全级别扫描：`--severity UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL`

### 2. GitLab 集成报告

扫描后会自动在 GitLab 显示：

- 项目 → **安全与合规 → 容器扫描**
- 合并请求 → 直接显示漏洞数量

### 3. 支持扫描 Go 代码依赖

Trivy 会自动扫描 `go.mod` / `go.sum` 里的第三方库漏洞，**不需要额外配置**。

------

## 四、如果你不用 Docker in Docker（自建 Runner）

把 build 阶段换成**使用宿主机 Docker**：

yaml











```
build-go-image:
  stage: build
  image: docker:latest
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $FULL_IMAGE .
    - docker push $FULL_IMAGE
  tags:
    - shell  # 你的 Runner 标签
```

------

## 五、最精简版本（只想快速跑扫描）

yaml











```
stages:
  - scan

trivy-scan:
  stage: scan
  image: aquasec/trivy:latest
  script:
    - trivy image YOUR_IMAGE_NAME:TAG --severity HIGH,CRITICAL --exit-code 1
```

------

## 六、常见问题

1. 无法拉取镜像

   - 确保 trivy 执行了 `trivy login`

   

2. 扫描成功但 GitLab 不显示报告

   - 检查 artifacts 配置：

   

   yaml

   

   

   

   

   

   ```
   artifacts:
     reports:
       container_scanning: gl-container-scanning-report.json
   ```

   

3. 想忽略某些漏洞

   

   在项目根目录创建 

   ```
   .trivyignore
   ```

   ：

   plaintext

   

   

   

   

   

   ```
   CVE-2024-XXXX
   CVE-2023-XXXX
   ```

   

------

## 七、效果总结

✅ 自动构建 Go 镜像

✅ 自动扫描高危漏洞

✅ 漏洞拦截 → 流水线失败

✅ GitLab 页面直接看安全报告

✅ 支持合并请求门禁（阻止不安全代码合并）





### 一、两种集成方式（任选其一）

#### 方式 1：使用 GitLab 内置容器扫描模板（最简）

GitLab 自带 Trivy 扫描模板，**一行代码启用**极狐GitLab：

yaml











```
# .gitlab-ci.yml
include:
  - template: Security/Container-Scanning.gitlab-ci.yml  # 官方模板

variables:
  # 要扫描的镜像（GitLab 容器仓库）
  CS_IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA
  # 扫描级别：只拦截高危/严重
  CS_SEVERITY_THRESHOLD: HIGH,CRITICAL
  # 使用 Trivy 作为扫描器
  CS_ANALYZER_IMAGE: aquasec/trivy:latest

# 可选：覆盖默认行为
container_scanning:
  stage: security
  needs: [build]  # 依赖构建任务
  allow_failure: false  # 发现漏洞则流水线失败
```

#### 方式 2：完全自定义 Trivy 任务（推荐，可控性强）

适合需要精细控制、私有镜像、缓存优化的场景：

yaml











```
stages:
  - build
  - security

# 全局变量
variables:
  IMAGE_NAME: my-go-app
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA
  FULL_IMAGE: $CI_REGISTRY/$CI_PROJECT_PATH/$IMAGE_NAME:$IMAGE_TAG
  TRIVY_CACHE_DIR: .trivy-cache
  TRIVY_NO_PROGRESS: "true"

# 1. 构建镜像（Go 项目示例）
build:
  stage: build
  image: docker:24-dind
  services:
    - docker:24-dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $FULL_IMAGE .
    - docker push $FULL_IMAGE
  only: [main, merge_requests]

# 2. Trivy 镜像扫描（核心）
trivy-scan:
  stage: security
  image: aquasec/trivy:latest
  needs: [build]
  variables:
    TRIVY_USERNAME: $CI_REGISTRY_USER
    TRIVY_PASSWORD: $CI_REGISTRY_PASSWORD
    TRIVY_AUTH_URL: $CI_REGISTRY
  script:
    # 预热漏洞库
    - trivy image --download-db-only
    # 生成 GitLab 兼容报告
    - trivy image
        --format gitlab
        --output gl-container-scanning-report.json
        --severity HIGH,CRITICAL
        --exit-code 1  # 漏洞 → 失败
        --ignore-unfixed  # 忽略未修复漏洞（可选）
        $FULL_IMAGE
  # 缓存加速
  cache:
    paths:
      - $TRIVY_CACHE_DIR
  # 报告上传 GitLab
  artifacts:
    reports:
      container_scanning: gl-container-scanning-report.json
    when: always
  only: [main, merge_requests]
```

------

### 二、关键参数说明（必懂）

- `--severity HIGH,CRITICAL`：只拦截高危 / 严重漏洞
- `--exit-code 1`：发现漏洞 → 流水线失败（质量门禁）
- `--ignore-unfixed`：忽略暂无修复方案的漏洞（避免误阻塞）
- `TRIVY_CACHE_DIR`：缓存漏洞库，**大幅提速**
- `artifacts.reports.container_scanning`：报告自动同步到 GitLab 安全面板

------

### 三、私有镜像仓库认证

如果扫描**非 GitLab 私有镜像**，添加认证：

yaml











```
variables:
  TRIVY_USERNAME: "你的仓库用户名"
  TRIVY_PASSWORD: "你的仓库密码"  # 存在 GitLab CI Variables 中（加密）
  TRIVY_AUTH_URL: "https://registry.example.com"
```





适用场景：

- Go 项目镜像已经打好
- 部署在 K8s
- 想要：先发布 10% 流量 → 观察无异常 → 全量发布
- 或：新版本只给内部测试，不影响线上

------

# 一、灰度发布（Rollout）两种主流方案

## 1）流量灰度（推荐，最安全）

- 旧版本：90%

- 新版本：10%

  

  通过 Service + 多个 Deployment + Ingress/ServiceMesh 切流量。

## 2）副本灰度（简单快速）

- 总副本 5 个

- 旧版本 4 个，新版本 1 个

  

  靠 Pod 数量比例实现灰度。

------

# 二、前置条件

1. K8s 集群（EKS/ACK/K3s/KubeSphere）
2. 项目已 Deployment + Service 正常运行
3. GitLab CI 能访问 K8s
4. 镜像已推到仓库（GitLab 镜像仓库 / Harbor/DockerHub）

------

# 三、详细操作步骤（完整可照做）

## 步骤 1：准备两个 Deployment（v1 旧版 /v2 新版）

### 1. 旧版本 deployment（线上稳定版）

```
deploy-v1.yaml
```

yaml











```
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
      version: v1
  template:
    metadata:
      labels:
        app: myapp
        version: v1
    spec:
      containers:
      - name: myapp
        image: $CI_REGISTRY_IMAGE:v1
        ports:
        - containerPort: 8080
```

### 2. 新版本 deployment（灰度版本）

```
deploy-v2.yaml
```

yaml











```
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-v2
spec:
  replicas: 1      # 小流量
  selector:
    matchLabels:
      app: myapp
      version: v2
  template:
    metadata:
      labels:
        app: myapp
        version: v2
    spec:
      containers:
      - name: myapp
        image: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA
        ports:
        - containerPort: 8080
```

### 3. Service（同时绑定 v1 和 v2）

```
service.yaml
```

yaml











```
apiVersion: v1
kind: Service
metadata:
  name: myapp-svc
spec:
  selector:
    app: myapp    # 同时匹配 v1 和 v2
  ports:
  - port: 80
    targetPort: 8080
```

> 流量会**自动按 Pod 数量比例分配**。
>
> 例如 v1:3、v2:1 → v2 获得 25% 流量。

------

# 四、实现精确流量切分（10% / 20% / 50%）

要精确控制流量比例，不能只靠副本数，有三种方案：

## 方案 A：使用 Nginx Ingress 做灰度（最简单）

通过 `nginx.ingress.kubernetes.io/canary` 注解实现流量切分。

```
ingress-canary.yaml
```

yaml











```
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-canary
  annotations:
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-weight: "10"  # 10% 流量到 v2
spec:
  rules:
  - host: api.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: myapp-svc-v2
            port:
              number: 80
```

> 权重 0~100，想多少流量就填多少。

## 方案 B：使用 Istio/ServiceMesh（企业级）

定义 `VirtualService` + `DestinationRule` 控制版本流量。

## 方案 C：使用 Argo Rollout（最强灰度，自带暂停 / 自动分析 / 回滚）

下面重点讲 **Argo Rollout**，这是 Kubernetes 生态最标准的 Rollout 方案。

------

# 五、Argo Rollout 完整灰度方案（企业标准）

## 1. 安装 Argo Rollouts

bash



运行









```
kubectl create namespace argo-rollouts
kubectl apply -n argo-rollouts -f https://github.com/argoproj/argo-rollouts/releases/latest/download/install.yaml
```

## 2. 编写 Rollout 资源（替代 Deployment）

```
rollout.yaml
```

yaml











```
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: myapp
spec:
  replicas: 4
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: $IMAGE
        ports:
        - containerPort: 8080

  # 灰度策略核心
  strategy:
    canary:
      # 第一步：启动 20% 副本
      steps:
      - setWeight: 20
      # 暂停，人工确认再继续
      - pause: {}
      # 确认后全量
      - setWeight: 100
```

## 3. 发布命令

bash



运行









```
kubectl apply -f rollout.yaml
```

## 4. 查看灰度状态

bash



运行









```
kubectl argo rollouts get rollout myapp
```

## 5. 继续发布（人工确认）

bash



运行









```
kubectl argo rollouts promote myapp
```

## 6. 一键回滚（异常立即执行）

bash



运行









```
kubectl argo rollouts undo myapp
```

## 7. 自动回滚（基于监控 / 错误率）

可对接 Prometheus，5xx 增高自动回滚。

------

# 六、GitLab CI 集成 Rollout 发布（直接可用）

## .gitlab-ci.yml

yaml











```
stages:
  - build
  - deploy_canary
  - approve
  - deploy_full

variables:
  IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA

build:
  stage: build
  script:
    - docker build -t $IMAGE .
    - docker push $IMAGE

# 灰度发布 20%
deploy_canary:
  stage: deploy_canary
  script:
    - kubectl apply -f rollout.yaml
    - kubectl argo rollouts status myapp

# 人工确认卡点
approve:
  stage: approve
  when: manual
  script:
    - echo "approve to continue"

# 全量发布
deploy_full:
  stage: deploy_full
  script:
    - kubectl argo rollouts promote myapp
```







# 一、整体架构（最标准的 GitOps 流程）

1. **代码提交** → GitLab CI

2. CI 做：**单元测试 → Sonar 扫描 → Trivy 镜像扫描 → 推镜像**

3. CI 自动**更新 GitOps 仓库镜像版本**

4. **ArgoCD** 自动同步 K8s 配置

5. Argo Rollouts

    执行

   灰度发布

   （金丝雀）：

   - 先发布 20% 流量
   - 人工确认
   - 全量发布
   - 异常一键回滚

   

------

# 二、项目结构（必须按这个结构）

plaintext











```
your-go-project/
├── frontend/            # 前端模块
│   ├── Dockerfile
│   ├── main.go
│   └── ...
├── backend/             # 后端模块
│   ├── Dockerfile
│   ├── main.go
│   └── ...
├── .gitlab-ci.yml       # 核心 CI 流水线
└── gitops/              # GitOps 配置（给 ArgoCD 使用）
    ├── frontend/
    │   ├── rollout.yaml
    │   └── service.yaml
    └── backend/
        ├── rollout.yaml
        └── service.yaml
```

------

# 三、第 1 步：Argo Rollouts 灰度配置（最关键）

## 1. frontend 灰度配置

```
gitops/frontend/rollout.yaml
```

yaml











```
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: frontend
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: ${CI_REGISTRY}/your-project/frontend:main   # 镜像会被CI自动更新
        ports:
        - containerPort: 8080

  # 灰度发布策略（金丝雀）
  strategy:
    canary:
      steps:
      - setWeight: 20       # 第一步：20% 流量
      - pause: {}           # 暂停，人工确认继续
      - setWeight: 100      # 确认后全量
```

## 2. backend 灰度配置

```
gitops/backend/rollout.yaml
```

yaml











```
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: backend
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: ${CI_REGISTRY}/your-project/backend:main
        ports:
        - containerPort: 8080

  strategy:
    canary:
      steps:
      - setWeight: 20
      - pause: {}
      - setWeight: 100
```

## 3. Service（供灰度流量分配）

```
gitops/frontend/service.yaml
```

yaml











```
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 8080
gitops/backend/service.yaml
```

yaml











```
apiVersion: v1
kind: Service
metadata:
  name: backend-svc
spec:
  selector:
    app: backend
  ports:
  - port: 80
    targetPort: 8080
```

------

# 四、第 2 步：GitLab CI 流水线（完整最终版）

## `.gitlab-ci.yml`（直接复制使用）

yaml











```
stages:
  - test
  - build
  - scan
  - deploy

# 全局变量（你只需改这里）
variables:
  CI_REGISTRY: gitlab.example.com:5000  # 你的GitLab镜像仓库
  PROJECT_PATH: your-group/your-project
  FRONTEND_IMAGE: $CI_REGISTRY/$PROJECT_PATH/frontend
  BACKEND_IMAGE: $CI_REGISTRY/$PROJECT_PATH/backend
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA
  GITLAB_USER: "ci-bot"
  GITLAB_EMAIL: "ci@example.com"

# 缓存 Go 依赖
cache:
  paths:
    - .go/pkg/mod/

# 1. 单元测试
go-test:
  stage: test
  image: golang:1.22
  script:
    - cd frontend && go test -v ./...
    - cd ../backend && go test -v ./...

# 2. 构建并推送 frontend 镜像
build-frontend:
  stage: build
  image: docker:24-dind
  services:
    - docker:24-dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - cd frontend
    - docker build -t $FRONTEND_IMAGE:$IMAGE_TAG .
    - docker push $FRONTEND_IMAGE:$IMAGE_TAG

# 3. 构建并推送 backend 镜像
build-backend:
  stage: build
  image: docker:24-dind
  services:
    - docker:24-dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - cd backend
    - docker build -t $BACKEND_IMAGE:$IMAGE_TAG .
    - docker push $BACKEND_IMAGE:$IMAGE_TAG

# 4. Trivy 镜像安全扫描
trivy-scan:
  stage: scan
  image: aquasec/trivy:latest
  script:
    - trivy image $FRONTEND_IMAGE:$IMAGE_TAG --severity HIGH,CRITICAL --exit-code 1
    - trivy image $BACKEND_IMAGE:$IMAGE_TAG --severity HIGH,CRITICAL --exit-code 1

# 5. 更新 GitOps 镜像 → 触发 ArgoCD 部署 + 灰度发布
deploy-gitops:
  stage: deploy
  image: alpine/git
  script:
    - git config --global user.name "$GITLAB_USER"
    - git config --global user.email "$GITLAB_EMAIL"
    - git clone https://gitlab.example.com/your-group/gitops-repo.git
    - cd gitops-repo

    # 更新 frontend 镜像
    - sed -i "s|image:.*frontend.*|image: $FRONTEND_IMAGE:$IMAGE_TAG|g" frontend/rollout.yaml

    # 更新 backend 镜像
    - sed -i "s|image:.*backend.*|image: $BACKEND_IMAGE:$IMAGE_TAG|g" backend/rollout.yaml

    - git add .
    - git commit -m "CI auto update image tag $IMAGE_TAG"
    - git push
```

------

# 五、第 3 步：ArgoCD 配置（对接 GitLab）

## 1. 创建 ArgoCD 应用

yaml











```
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: go-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://gitlab.example.com/your-group/gitops-repo.git
    targetRevision: main
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
```

## 2. 命令创建

bash



运行









```
kubectl apply -f argocd-application.yaml
```

访问 ArgoCD：`https://argocd.example.com`

------

# 六、第 4 步：灰度发布完整操作流程（真实上线步骤）

## 1. 提交代码

bash



运行









```
git push
```

## 2. GitLab CI 自动执行

- 测试
- 构建镜像
- Trivy 安全扫描
- 更新 GitOps 镜像

## 3. ArgoCD 自动同步

触发 **Argo Rollouts** 灰度发布

## 4. 查看灰度状态

bash



运行









```
kubectl argo rollouts get rollout frontend
kubectl argo rollouts get rollout backend
```

## 5. 观察 20% 灰度版本

- 日志
- 监控
- 接口测试

## 6. 人工确认继续发布

bash



运行









```
kubectl argo rollouts promote frontend
kubectl argo rollouts promote backend
```

## 7. 完成 100% 全量发布

## 8. 异常一键回滚

bash



运行









```
kubectl argo rollouts undo frontend
kubectl argo rollouts undo backend
```

------

# 七、你只需要改 4 个地方

1. GitLab 地址：`gitlab.example.com`
2. 项目路径：`your-group/your-project`
3. GitOps 仓库地址
4. 命名空间（默认 default）
